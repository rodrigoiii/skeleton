<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Flash</title>
</head>
<body>
    {% if flash.getFirstMessage('pizza') %}
        <p>{{ flash.getFirstMessage('pizza') }}</p>
    {% endif %}

    <form method="POST">
        <button>Submit the form to show flash message</button>
    </form>
</body>
</html>
